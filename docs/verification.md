# 验收状态与迁移边界

> 最新审阅（2026-09-24）：本轮现有测试全部通过，但专项 API / 浏览器检查确认了 8 项缺陷，包括跨空间数据访问、5000 条样本建任务失败、提交后结果覆盖、重复消费和执行页问题。完整证据、架构评价与功能进度见 [项目审阅报告](reviews/2026-09-24-project-review.md)。以下 2026-09-22 验收记录保留为历史证据，不代表上述缺陷已修复。

核对日期：2026-09-22。依据：当前源码、[总规划](plans/0002-2026-09-22-规划-迁移总规划.md)、[M5 快照](handoff/0009-2026-09-22-状态快照-M5完成.md)、[M6 准备快照](handoff/0010-2026-09-22-状态快照-M6部署准备完成.md)。

## 结论

**核心业务流程已实现并通过本地验收，但不能认定所有规划功能完整或迁移全部完成。** M0–M5 在历史快照中标为完成，表示当时实现与验收范围完成；逐项对照总规划仍有下面列出的差异。M6 仅部署准备完成，服务器上线、真实 CI、历史数据处理与业务切换未验收。

已验收核心流程包括：登录与角色权限、用户与空间管理、标注工具创建、AI 配置、JSONL 数据集直传与解析、任务派发、人工标注、审核通过 / 驳回 / 重标、AI 阶段、结果导出、自动保存、通知、截止提醒、任务暂停 / 恢复 / 结束、用户禁用 / 启用。

## 已有验证证据（上一轮 M6 本地验收）

| 验证层 | 结果 | 可复查位置 |
|---|---|---|
| 后端单元 / 集成 | 19 文件、191 用例通过；releaseId 补充后相关 17 用例再次通过 | `apps/server/tests/`、快照 0010 §3 |
| 前端单元 / 组件 | 8 文件、57 用例通过 | `apps/web/src/**/*.test.ts(x)`、快照 0010 §3 |
| 编译和静态检查 | 两仓 lint / typecheck / build 通过；前端仍有 75 个既有 warning | 快照 0010 §3 |
| 业务 API 冒烟 | M1 30/30、M2 37/37、M3 97/97 | `apps/server/scripts/smoke/` |
| 真实浏览器 | M3 7 步、M4 5 步、M5 6 步通过 | `apps/web/e2e/`；此前 M0–M2 见各里程碑快照 |
| 发布与回滚脚本 | ShellCheck、Linux 夹具、失败回滚、并发锁和路径校验通过 | `apps/server/scripts/test-activate.sh`；PM2/npm 为模拟 |
| Nginx 和发布冒烟 | 本地隔离容器下语法、反代、静态资源、版本身份核对通过 | 快照 0010 §3；临时证书 / 回环端口 |
| 配对发布包 | 357 文件；无环境凭据，归档校验通过 | `artifacts/m6-ready-20260922.tar.gz` 及 `.sha256` |

API 与浏览器验收针对本地真实前后端及 PG / Redis / MinIO。AI 冒烟使用本地模拟 OpenAI 兼容服务，不能据此声称生产模型效果、外部供应商可用性已验收。自动化测试通过也不等于所有边界和业务需求均已覆盖。

## 尚未完成或存在差异

| 项目 | 实际状态 / 证据 |
|---|---|
| 数据集 CSV 导入 | 总规划 M2 写了 jsonl/csv；实现 `src/modules/dataset/enums.ts` 仅 JSONL，解析服务仅 `parseJsonlStream`。M2 实际验收包含 CSV 被拒绝。CSV 导入是尚未实现的规划差异；与结果 CSV 导出、用户 CSV 导入不同。 |
| 标注工具更新 | 总规划 M1 写了创建 / 更新 / 列表；实际前后端只有 create / list / detail，没有更新端点。M1 快照也只验收这三个接口。 |
| P16 / P17 | 规则预审引擎、审核操作历史是可选项，尚未实现。 |
| 其它已知遗留 | AI 耗尽重试后的手动重派、审核页草稿自动保存未实现；已读通知无定期清理；截止扫描每批最多 500 case；禁用用户跨实例缓存最多 10s；前端既有警告。 |
| 历史数据 | 没有 LabelHub 历史数据自动迁移工具，也没有迁移验数。早期报告曾提出从新数据起步；实际切换前仍须明确保留旧库还是另做迁移。 |
| 生产部署 | 服务器未就绪，未部署。真实域名 / TLS / 对象存储直传 / 云权限 / PM2 / systemd / 重启恢复 / 备份恢复待验收。 |
| GitHub CI/CD | 代码与工作流提交到私有仓库 [xilele777/markflow](https://github.com/xilele777/markflow)。云端 CI 结果以 [Actions](https://github.com/xilele777/markflow/actions/workflows/ci.yml) 为准；部署变量明确为 false，未部署。 |
| 业务切换 | 未切换旧系统流量，LabelHub 未封存；切换后观察两周的阶段未开始。 |

上线时还需按 [部署手册](server/deployment.md) 完成实机检查。迁移关闭前应逐项确认以上差异是补齐、延期还是正式排除，不能只依据里程碑标题判断全部完成。

## 目录整理与合仓后的复验

目录整理结果见 [快照 0011](handoff/0011-2026-09-22-状态快照-目录整理与验收边界.md)。API / 浏览器 / Linux 运维演练沿用上一轮证据，本轮没有重复执行，也没有部署。发布包保留原版本与校验值，不因为文档整理冒称为新的发布版本。

合仓后的安装、构建、测试、历史校验和发布包验证见 [快照 0012](handoff/0012-2026-09-22-状态快照-monorepo合并.md)。仓库结构调整不补齐上述业务缺项，也不等于完成 M6 上线。
