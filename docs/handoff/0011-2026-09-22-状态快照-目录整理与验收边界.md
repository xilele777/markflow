# 快照 0011 · 目录整理与验收边界

日期：2026-09-22。上一篇：[M6 部署准备](0010-2026-09-22-状态快照-M6部署准备完成.md)。当前入口：[docs/README.md](../README.md)。

## 1. 当前结论

用户要求核对功能与迁移是否完成，并整理项目目录。复核发现：核心业务流程已有本地验收，但尚不能称所有规划功能完整或迁移结束。

- M0–M5 以往快照的“完成”是当时验收范围完成；总规划 M1 的标注工具更新、M2 的 CSV 数据集导入尚未实现，已在总规划与 [verification.md](../verification.md) 显式标注，保留原计划待收口。
- M5 的可选规则预审 / 审核历史未实现，其它遗留见 verification.md。
- M6 仅部署准备完成。未部署、未 push、未启用 CD、未切换流量、未迁移历史数据、未封存 LabelHub。

## 2. 目录整理

新增根 README 和 docs 索引、验收状态、目录规范。详细 [旧路径对照](../structure.md)。

- 根两份早期报告移入 `docs/reference/`；被替代的两份计划归入 `docs/archive/plans/` 并标注失效。
- 后端长 README、DEPLOY、接口冒烟说明分别归入 `markflow-server/docs/development.md`、`deployment.md`、`testing.md`，根 README 改为入口。
- 前端浏览器说明归入 `docs/testing.md`；设计交付包移至 `docs/design/`，更新索引与忽略规则；本地 JSONL 样例移入 `examples/datasets/` 和 `examples/users/`，仍不入库。
- 删除未被引用的 `PagePlaceholder.tsx`（含两个旧占位组件）。无路由、接口、数据迁移或业务行为修改。
- 原正式解包目录与归档逐文件核对，357 文件一致。两个生成物目录的删除被自动审批以 `blocked by policy` 拦截，改为归入 `markflow-server/artifacts/archive/`，未删除。
- 保留 `.tar.gz` / `.sha256`、本地配置、依赖、现用构建输出和数据库；LabelHub 与 submission 未改动。

两个应用保留独立 Git 仓库与现有根路径，仓库文档随代码版本管理，根 docs 只统一索引跨仓库资料。根 README / docs 本身没有 Git，需要工作区备份。历史 handoff 不改写。

本轮本地提交（均未 push）：后端 `afc71f2 docs: organize development and deployment guides`；前端 `5a71ec2 chore: organize docs and remove unused page placeholders`。

## 3. 本轮实际复验

| 检查 | 结果 |
|---|---|
| 后端 lint / typecheck / test / build | 全通过；19 文件、191 用例 |
| 前端 lint / typecheck / test / build:check | 全通过；8 文件、57 用例；75 条既有 lint warning |
| 前端体积门禁 | 入口 gzip 271.2 KiB / 300 KiB，最大 chunk 176.5 KiB / 200 KiB；Vite 既有大 chunk 提示仍在 |
| 发布归档校验 | SHA256 不变；移动前归档与解包目录 357 文件逐文件一致 |
| 当前文档链接 | 77 个本地链接检查通过，无失效目标 |
| 设计资料移动 | 45 个原始文件与 Git 原版本内容一致（7 个仅有原有 CRLF 检出差异）；设计 README 另加历史资料说明 |
| 本轮构建与原发布包 | 后端 194 个 dist 文件、前端 155 个静态文件逐字节一致；发布元数据仍属原版本 |

本轮没有重跑 API / Chrome / Linux 运维演练；这些结果引用快照 0010，不混作本轮新结果。后端测试使用专用测试库与测试队列；未重置开发库。

## 4. 发布包与后续

原发布包仍是 `markflow-server/artifacts/m6-ready-20260922.tar.gz`，SHA256：`5765b7919099c19d6458bf77d7a45c4d52fcc5de1efd8806e9f87d51c4a76443`。包内前后端提交仍为 `f3906f0` / `f45ec73`，不得把本轮目录整理后的提交号写入旧 manifest 或改写校验值。

服务器未准备时继续保持 CD 关闭。下一阶段先明确是否补齐 CSV 导入、标注工具更新及可选项，并确认历史数据策略；实机验收遵循 [新位置的部署手册](../../markflow-server/docs/deployment.md)。
