# 快照 0013 · GitHub 私有仓库建立

日期：2026-09-22。上一篇：[monorepo 合并](0012-2026-09-22-状态快照-monorepo合并.md)。

用户明确授权创建新的 GitHub 仓库并 push。使用当前登录账号 `xilele777`，创建私有仓库 [xilele777/markflow](https://github.com/xilele777/markflow)，本地 origin 指向 `https://github.com/xilele777/markflow.git`。推送 main 与两个历史标签 `migration/server-before-monorepo`、`migration/web-before-monorepo`，保留双方原提交历史。

此前尚未提交的 TypeScript 修复已保存为 `c0492ed fix(web): remove deprecated baseUrl option`：移除 baseUrl，使用 `@/* -> ./src/*`；已在上一轮通过 TypeScript 6 类型检查、前端构建和体积预算。

推送前检查当前跟踪文件不包含本地 `.env`、依赖、构建产物、样例 JSONL、发布归档或 `.migration-backup/`；原提交历史中没有超过 90 MiB 的文件。历史备份和本地发布候选包不上传。

**服务器仍未准备好，不部署、不切换流量。** 创建仓库后、首次 push 前已明确设置仓库变量 `MARKFLOW_DEPLOY_ENABLED=false`，并读回确认。新仓库保持私有，不把旧前端 upstream 当作发布目标。

main 推送会自动触发统一 CI；[云端运行记录](https://github.com/xilele777/markflow/actions/workflows/ci.yml) 是 CI 状态的实时来源。本快照不宣称完成实机部署、恢复演练或 M6 业务切换；这些仍按 [部署手册](../server/deployment.md) 验收。
