# 快照 0014 · markflow 改名

日期：2026-09-24。用户确定英文名称为 `markflow`，不使用中文品牌名。

## 已完成

- GitHub 私有仓库已改名为 [xilele777/markflow](https://github.com/xilele777/markflow)，本地 `origin` 已更新；提交历史和两个历史标签保留，没有新建空仓库或改写历史。
- GitHub 部署开关改为 `MARKFLOW_DEPLOY_ENABLED=false`，旧开关已移除；仓库当前没有需要迁移的 Actions secrets。没有部署。
- 根包名改为 `markflow`，应用包名改为 `markflow-server` / `markflow-web`，同步三份锁文件。
- 登录页、侧栏、页面标题统一显示小写 `markflow`；移除原来的双语言品牌重复展示。
- 环境变量、浏览器存储键、iframe 消息标识、Prometheus 指标、Compose、PM2、Nginx、systemd、发布与备份脚本、CI/CD、启动配置与测试同步改名。
- README、当前和历史文档的项目名称、相关文件名及链接同步更新。历史文档中的品牌统一显示当前名称；当时的原始文件和措辞仍可按原 Git 提交查询。
- 数据库业务表改为 `markflow_label_tool`、`markflow_dataset`、`markflow_dataset_version`、`markflow_dataset_sample`，TypeScript schema 与全部查询同步更新。

## 数据衔接

新增 `0006_markflow_names` 数据库迁移：重命名旧表、序列及索引，保持行内容、主键、序列和引用。V1 / V2 保留原始 SQL 标识，供已有数据库升级和迁移回退使用；新安装也会经过 V6 得到新表名。不要为了消除搜索结果而改掉历史 SQL 或 V6 中的来源前缀。

本机基础设施已处理：

- 原 PostgreSQL / Redis / MinIO 停止后，将三个数据卷复制到 `markflow_pgdata`、`markflow_redisdata`、`markflow_miniodata`。
- 新实例使用 `markflow-postgres`、`markflow-redis`、`markflow-minio`；数据库 / 角色为 `markflow`，测试数据库为 `markflow_test`。
- 队列前缀已迁移，存储对象复制到 `markflow` 桶，MinIO 访问身份同步更新。
- 本地 `.env` 已同步。密码、JWT 密钥和配置加密密钥保持原值，不因品牌替换而改变凭据。
- 启动业务进程和运行测试前，对开发库的 14 张业务表逐表比较行数和内容摘要，全部一致；包含 42,209 条数据样本。74 个 Redis 键在迁移核验时数量一致；277 个对象的 key、大小、ETag 一致。
- 原容器和原数据卷保留为停止状态的本地回退副本；原桶仍作为复制卷内的备份保留，不再被新配置使用。旧发布归档、Git 备份和历史截图不重新生成。

迁移操作与数据核验记录位于被忽略的 `artifacts/markflow-rename/`。其中环境备份含本地凭据，不应提交或打包。启动命令仍从仓库根目录执行 `npm run infra:up`、`npm run dev:server`、`npm run dev:web`。

浏览器存储命名空间已变更，原登录状态不自动继承；重新登录后写入 `markflow.token` 和 `markflow.spaceCode`。

## 本轮验证

| 检查 | 结果 |
|---|---|
| lint / typecheck | 通过；前端既有 75 个 warning，没有新增 error |
| 根 `npm test` | 通过：打包 3、后端 192、前端 57，共 252 用例 |
| 新增迁移回归 | 行、序列默认值、外键引用保持；重复执行无损；可回退 |
| 两端生产构建及体积预算 | 通过 |
| Linux 发布脚本语法与夹具 | 通过，含激活、失败回滚、并发锁等模拟场景 |
| 改名后 `npm run infra:up` | 通过，三个服务健康，目标桶可用 |
| 真实浏览器 | 页面标题、登录品牌、侧栏均为 markflow；登录成功进入数据集页，新存储键生效 |
| API health | 测试库实例的 PostgreSQL / Redis / 存储 / 队列全部就绪 |
| 开发数据迁移核验 | 表内容摘要一致，存储对象一致 |

改名没有修复上一轮审阅报告中的 8 项业务缺陷，也不代表生产上线完成。本轮代码与文档作为品牌调整提交保存，尚未推送；GitHub 仓库名称与部署变量已实际更新。
