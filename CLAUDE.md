# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目身份

灵枢前端（LingShu Web）= 灵枢数据生产与协同平台的 Web 前端，面向管理与标注 / 质检两类使用场景。纯前端仓库；对接的接口契约由后端方提供，**不要扫描其它目录去推断接口**。

## 技术栈

React 18 + TypeScript + Vite + Ant Design。状态管理、请求层、目录结构、构建配置等尚未定。

## 设计纪律（动手前必读）

- 样式决策不交给 AI 自由发挥，靠 token + AntD 组件 + 文档锁定外观。
- 颜色只用 `docs/standards/配色规范.md` 的 token，禁止硬编码色值；外壳永远中性，颜色只表达「状态」「分类」两类信息。
- 用 AntD 组件 + 定制主题 token 出外观，不手搓 CSS override、不堆内联 style。
- **全站只用 `docs/standards/组件清单.md` 的封装组件拼装**；页面照 `页面模板.md` 对号入座；状态 / 文案以 `状态映射.md` / `文案规范.md` 为准。
- **视觉方向**：工坊·浅色（中性外壳 + 强调蓝 `#2f6df0` + IBM Plex 全家族 + 实心蓝主按钮），详见 `配色规范.md`。

## 协作流程（每次都遵守）

- 先问后写：不明确处先提问，尽量给 A / B / C 选项，澄清后才写代码。
- 按模块 / 功能点推进，一次一个。
- **每个页面分两步走**：
  1. **先 mock 出页面**：用 mock 数据把页面做出来，跑通布局 / 交互 / 空·加载·错三态，dev server 实测对齐设计。
  2. **再绑定真实接口、与后端联调**：切掉 mock、改调 `features/<x>/api.ts` 的真实接口函数，联调通过才算完成。
  - 接口函数**始终按真实契约**写在 `features/<x>/api.ts`（字段 / 路径以《接口文档》为准）；mock 只用于第①步出页面，第②步删除。后端未起（隧道 / 服务）时停在第①步，起来后再做第②步。
- 禁止自动 git commit / push；用户明确说"可以提交"后才提交。
- 除非用户明确要求，否则不执行 安装 / 构建 / dev / 测试 命令。

## 当前阶段

- **已定**：技术栈、视觉方向（工坊·浅色）、配色 / 菜单 / 组件清单 / 页面模板 / 状态映射 / 文案 / 工程结构 / 接口层 / 权限可见性 规范。
- **未定**：Puck 搭建（标注工具 pageSchema）方案 —— 暂不做。
- 权限可见性表为**建议、待确认**（细节见该文档末尾）。
- 未定项**动手前必须先问**，不要自创或套用通用模板。

## 文档按需加载

动手前先查 `docs/INDEX.md`，按场景读对应规范：

- 写任何页面前 → `docs/standards/组件清单.md`、`页面模板.md`
- 带颜色 / 状态标签 → `配色规范.md`、`状态映射.md`
- 左侧导航 → `菜单栏.md`；界面文字 → `文案规范.md`
