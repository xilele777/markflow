import { PagePlaceholder } from '@/app/layout/PagePlaceholder';

// 数据集详情（《页面模板.md》二：头卡 + 版本列表，行操作开 Drawer）。待实现。
export default function DatasetDetailPage() {
  return <PagePlaceholder title="数据集详情" />;
}
