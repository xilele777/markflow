import { FullscreenPlaceholder } from '@/app/layout/PagePlaceholder';

// 标注执行页（全屏，《页面模板.md》四：ExecToolbar + ExecFrame）。待实现。
export default function LabelExecPage() {
  return <FullscreenPlaceholder title="标注执行页" />;
}
