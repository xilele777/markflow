import { PagePlaceholder } from '@/app/layout/PagePlaceholder';

// 新建标注任务（《页面模板.md》三：基本信息 → 流程编排 → 人员/AI 分配）。待实现。
export default function CaseNewPage() {
  return <PagePlaceholder title="新建标注任务" />;
}
