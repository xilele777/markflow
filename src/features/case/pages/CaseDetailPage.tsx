import { PagePlaceholder } from '@/app/layout/PagePlaceholder';

// 标注任务详情（《页面模板.md》二：头卡 + StageProgressBar）。待实现。
export default function CaseDetailPage() {
  return <PagePlaceholder title="标注任务详情" />;
}
