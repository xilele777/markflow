name: Deploy (disabled until server is ready)

on:
  workflow_dispatch:
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]

permissions:
  contents: read

concurrency:
  group: markflow-production
  cancel-in-progress: false

jobs:
  prepare:
    # 保持未设置/false。只有服务器验收准备完成后才启用；PR、失败 CI 不触发。
    if: >-
      vars.MARKFLOW_DEPLOY_ENABLED == 'true' &&
      ((github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') ||
       (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_repository.full_name == github.repository))
    runs-on: ubuntu-latest
    env:
      RELEASE: release-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            apps/server/package-lock.json
            apps/web/package-lock.json
      - name: Backend checks on the exact revision
        working-directory: apps/server
        run: |
          npm ci
          npm run lint
          npx prettier --check src tests
          npm run typecheck
          bash -n scripts/activate.sh scripts/smoke.sh scripts/backup-pg.sh scripts/test-activate.sh
          shellcheck scripts/activate.sh scripts/smoke.sh scripts/backup-pg.sh scripts/test-activate.sh
          bash scripts/test-activate.sh
          cp .env.example .env
          npm run infra:up
          npm test
          npm run build
      - name: Frontend checks on the same revision
        working-directory: apps/web
        run: |
          npm ci
          npx eslint . --max-warnings 100
          npm run typecheck
          npm test
          npm run build:check
      - name: Verify monorepo packaging
        run: npm run test:release
      - name: Assemble immutable release from one commit
        run: |
          node apps/server/scripts/package-release.mjs apps/web artifacts "$RELEASE"
          tar -C artifacts -czf "artifacts/$RELEASE.tar.gz" "$RELEASE"
          (cd artifacts && sha256sum "$RELEASE.tar.gz" > "$RELEASE.tar.gz.sha256")
      - uses: actions/upload-artifact@v4
        with:
          name: release
          path: |
            artifacts/release-*.tar.gz
            artifacts/release-*.tar.gz.sha256
          retention-days: 14
      - name: Stop disposable CI infrastructure
        if: always()
        working-directory: apps/server
        run: |
          if [[ -f .env ]]; then npm run infra:down; fi

  activate:
    needs: [prepare]
    runs-on: ubuntu-latest
    environment: production
    env:
      RELEASE: release-${{ github.run_id }}-${{ github.run_attempt }}
      DEPLOY_HOST: ${{ secrets.MARKFLOW_DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.MARKFLOW_DEPLOY_USER }}
      DEPLOY_PORT: ${{ secrets.MARKFLOW_DEPLOY_PORT }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: release
          path: release
      - name: Install pinned SSH credentials
        env:
          SSH_KEY: ${{ secrets.MARKFLOW_DEPLOY_KEY }}
          KNOWN_HOSTS: ${{ secrets.MARKFLOW_DEPLOY_KNOWN_HOSTS }}
        run: |
          [[ "$DEPLOY_HOST" =~ ^[A-Za-z0-9][A-Za-z0-9.-]*$ ]]
          [[ "$DEPLOY_USER" =~ ^[a-z_][a-z0-9_-]*$ ]]
          [[ "$DEPLOY_PORT" =~ ^[0-9]{1,5}$ ]]
          [[ -n "$SSH_KEY" && -n "$KNOWN_HOSTS" ]]
          install -m 700 -d ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/id_ed25519
          printf '%s\n' "$KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/id_ed25519 ~/.ssh/known_hosts
      - name: Upload, verify checksum, activate with automatic rollback
        working-directory: release
        run: |
          sha256sum -c "$RELEASE.tar.gz.sha256"
          ssh -o BatchMode=yes -o StrictHostKeyChecking=yes -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" 'mkdir -p /srv/markflow/incoming /srv/markflow/releases'
          scp -o BatchMode=yes -o StrictHostKeyChecking=yes -P "$DEPLOY_PORT" "$RELEASE.tar.gz" "$RELEASE.tar.gz.sha256" "$DEPLOY_USER@$DEPLOY_HOST:/srv/markflow/incoming/"
          ssh -o BatchMode=yes -o StrictHostKeyChecking=yes -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" "set -eu; cd /srv/markflow/incoming; sha256sum -c '$RELEASE.tar.gz.sha256'; test ! -e '/srv/markflow/releases/$RELEASE'; tar --no-same-owner -xzf '$RELEASE.tar.gz' -C /srv/markflow/releases; bash '/srv/markflow/releases/$RELEASE/scripts/activate.sh' '$RELEASE'"
      - name: Remove runner SSH key
        if: always()
        run: rm -f ~/.ssh/id_ed25519
